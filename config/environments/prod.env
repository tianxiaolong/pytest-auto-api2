# 生产环境配置文件
# ⚠️ 生产环境配置，请谨慎修改！

# 环境基本信息
TEST_ENV=prod
ENV_NAME=生产环境
PROJECT_NAME=pytest-auto-api2
TESTER_NAME=测试工程师

# 生产环境服务地址
PROD_HOST=https://api.example.com
PROD_APP_HOST=https://app.example.com
PROD_DB_URL=mysql://user:password@prod-db:3306/proddb

# 数据驱动配置
DATA_DRIVER_TYPE=yaml
YAML_DATA_PATH=data/yaml_data
EXCEL_DATA_PATH=data/excel_data

# 测试用例配置
REAL_TIME_UPDATE=False

# 通知配置（生产环境建议启用所有通知）
NOTIFICATION_TYPE=1,2,3
EXCEL_REPORT=True

# 钉钉通知配置（生产环境）
DING_TALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=prod_token
DING_TALK_SECRET=prod_secret

# 企业微信通知配置（生产环境）
WECHAT_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=prod_key

# 邮箱通知配置（生产环境）
EMAIL_SEND_USER=prod@example.com
EMAIL_HOST=smtp.example.com
EMAIL_STAMP_KEY=prod_password
EMAIL_SEND_LIST=team@example.com,manager@example.com,cto@example.com

# 飞书通知配置（生产环境）
LARK_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/prod_hook

# 数据库配置（生产环境 - 只读权限）
MYSQL_SWITCH=False
MYSQL_HOST=prod-mysql-readonly.example.com
MYSQL_USER=readonly_user
MYSQL_PASSWORD=readonly_password
MYSQL_PORT=3306
MYSQL_DATABASE=prod_db

# Python包镜像源配置
MIRROR_SOURCE=http://mirrors.aliyun.com/pypi/simple/
