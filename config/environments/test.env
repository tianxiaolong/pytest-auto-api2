# 测试环境配置文件
# 复制此文件并根据实际情况修改配置值

# 环境基本信息
TEST_ENV=test
ENV_NAME=测试环境
PROJECT_NAME=pytest-auto-api2
TESTER_NAME=测试工程师

# 测试环境服务地址
TEST_HOST=https://test-api.example.com
TEST_APP_HOST=https://test-app.example.com
TEST_DB_URL=mysql://user:password@test-db:3306/testdb

# 数据驱动配置
DATA_DRIVER_TYPE=yaml
YAML_DATA_PATH=data/yaml_data
EXCEL_DATA_PATH=data/excel_data

# 测试用例配置
REAL_TIME_UPDATE=False

# 通知配置
NOTIFICATION_TYPE=0
EXCEL_REPORT=False

# 钉钉通知配置（测试环境）
DING_TALK_WEBHOOK=https://oapi.dingtalk.com/robot/send?access_token=test_token
DING_TALK_SECRET=test_secret

# 企业微信通知配置（测试环境）
WECHAT_WEBHOOK=https://qyapi.weixin.qq.com/cgi-bin/webhook/send?key=test_key

# 邮箱通知配置（测试环境）
EMAIL_SEND_USER=test@example.com
EMAIL_HOST=smtp.example.com
EMAIL_STAMP_KEY=test_password
EMAIL_SEND_LIST=team@example.com

# 飞书通知配置（测试环境）
LARK_WEBHOOK=https://open.feishu.cn/open-apis/bot/v2/hook/test_hook

# 数据库配置（测试环境）
MYSQL_SWITCH=True
MYSQL_HOST=test-mysql.example.com
MYSQL_USER=test_user
MYSQL_PASSWORD=test_password
MYSQL_PORT=3306
MYSQL_DATABASE=test_db

# Python包镜像源配置
MIRROR_SOURCE=http://mirrors.aliyun.com/pypi/simple/
